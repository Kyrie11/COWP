version: v16.8.4
# Development promotion heuristics, not theoretical guarantees.
full_rebuild_gate:
  min_any_ncf_scene_rate: 0.40
  max_best_case_selected_false_safe_lower_bound: 0.55
  max_best_case_pbtr_lower_bound: 0.45
  min_hard_scene_ncf_recovery_rate: 0.20
  min_old_ncf_scene_retention: 0.95
  min_provenance_scene_coverage: 0.95

algorithm_attribution_gate:
  # Compare RMR-BCTE against a single-region control with identical jerk semantics.
  min_incremental_random_any_ncf_scene_rate: 0.03
  min_incremental_hard_scene_recovery_rate: 0.05
  min_rmr_unique_hard_recovery_rate: 0.05
