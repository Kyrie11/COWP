# V16.8.44 — Root-Conditioned Control-Reachable Responder Support

V43 reliable mandatory-gate result: 3/10 historical RVR rescues retained (FAIL), 9/9 RVR-induced avoided (PASS). BC-IARE late-bound querying is active but produces no BC-only selection on gate19; in lost rescues root-unrecoverable rejects dominate unsupported rejects. Therefore close further blocker-indexing enlargement and activate the preregistered root-conditioned control-reachable responder-support branch.

V44 augments a retained natural root only when its frozen finite response bank has no ego+environment-safe profile. It preserves root geometry and all probability/burden/certificate semantics, derives response duration from the identity root's unsafe-event support, searches within inherited physical acceleration bounds, and accepts only responses that pass unchanged burden, roadgraph, Waymax kinematics, current+shift ego/environment safety and joint CSP.

Preregistered fail-fast: lost7 must newly rescue >=2; then rescue10 must retain >=5/10; then induced9 must avoid >=7/9; only then run remaining29 and the full frozen six-item counterfactual48 gate. No 80-step horizon shortening.
